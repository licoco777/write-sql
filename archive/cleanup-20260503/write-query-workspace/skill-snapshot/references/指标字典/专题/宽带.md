# 宽带
> 业务板块: 专题 | 业务分类: 宽带 | 共 4 个指标

---

## 宽带T+n有效率

| 字段 | 值 |
|------|-----|
| 业务板块 | 专题 |
| 业务分类 | 宽带 |
| 统计周期 | 月 |
| 业务口径 | (未填写) |
| 业务口径责任人 | 邱礼佳 |
| 技术口径责任人 | 钟雨君 |
| CDAP生产流程 | 移动宽带质态监控需求 |

### 技术口径 (SQL)
```sql
--T+1有效率=value1/value2：
SELECT subst_id,subst_name,concat('T+',mm_diff) as t_bs,
count(case when is_yx='1' and is_kjzj='否' then serv_id else null end) as value1, -- 总体有效量
  count(case when is_kjzj='否' then serv_id else null end) as value2 -- 总体量
FROM view_ads_zt_kdx_list
WHERE  par_month_id = 202603 -- 统计月份
AND concat('T+',mm_diff) in ('T+0','T+1','T+2','T+3','T+6','T+12') and mm_diff = 1 -- 1个月前入网
GROUP BY subst_id,subst_name,mm_diff;
```

---

## 宽带T+n欠费率

| 字段 | 值 |
|------|-----|
| 业务板块 | 专题 |
| 业务分类 | 宽带 |
| 统计周期 | 月 |
| 业务口径 | (未填写) |
| 业务口径责任人 | 邱礼佳 |
| 技术口径责任人 | 钟雨君 |
| CDAP生产流程 | 移动宽带质态监控需求 |

### 技术口径 (SQL)
```sql
--T+1欠费率=value1/value2：
SELECT subst_id,subst_name,concat('T+',mm_diff) as t_bs,
  count(case when is_qf='是' and is_kjzj='否' then serv_id else null end) as value1, -- 总体欠费量
    count(case when is_kjzj='否' then serv_id else null end) as value2 -- 总体量
FROM view_ads_zt_kdx_list
WHERE par_month_id = 202603 -- 统计月份
AND concat('T+',mm_diff) in ('T+0','T+1','T+2','T+3','T+6','T+12') and mm_diff = 1 -- 1个月前入网
GROUP BY subst_id,subst_name,mm_diff;
```

---

## 宽带T+n停机率

| 字段 | 值 |
|------|-----|
| 业务板块 | 专题 |
| 业务分类 | 宽带 |
| 统计周期 | 月 |
| 业务口径 | (未填写) |
| 业务口径责任人 | 邱礼佳 |
| 技术口径责任人 | 钟雨君 |
| CDAP生产流程 | 移动宽带质态监控需求 |

### 技术口径 (SQL)
```sql
--T+1停机率=value1/value2：
SELECT subst_id,subst_name,concat('T+',mm_diff) as t_bs,
  count(case when is_stop='停机' and is_kjzj='否' then serv_id else null end) as value1, -- 总体停机量
    count(case when is_kjzj='否' then serv_id else null end) as value2 -- 总体量
FROM view_ads_zt_kdx_list
WHERE  par_month_id = 202603 -- 统计月份
AND concat('T+',mm_diff) in ('T+0','T+1','T+2','T+3','T+6','T+12') and mm_diff = 1 -- 1个月前入网
GROUP BY subst_id,subst_name,mm_diff;
```

---

## 宽带T+n拆机率

| 字段 | 值 |
|------|-----|
| 业务板块 | 专题 |
| 业务分类 | 宽带 |
| 统计周期 | 月 |
| 业务口径 | (未填写) |
| 业务口径责任人 | 邱礼佳 |
| 技术口径责任人 | 钟雨君 |
| CDAP生产流程 | 移动宽带质态监控需求 |

### 技术口径 (SQL)
```sql
--T+1拆机率=value1/value2：
SELECT subst_id,subst_name,concat('T+',mm_diff) as t_bs,
count(case when is_cancel_user_1='是' and is_kjzj='否' then serv_id else null end) as value1, -- 总体拆机量
  count(case when is_kjzj='否' then serv_id else null end) as value2 -- 总体量
FROM view_ads_zt_kdx_list
WHERE par_month_id = 202603 -- 统计月份
AND concat('T+',mm_diff) in ('T+0','T+1','T+2','T+3','T+6','T+12') and mm_diff = 1 -- 1个月前入网
GROUP BY subst_id,subst_name,mm_diff;
```

---
