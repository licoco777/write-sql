请教下，能帮忙取一下近三个月，机主年龄大于等于65岁的所有订单不？

星期一16:03

我先按这个理解：

- 查什么：订单明细，不是汇总数量。
- 查谁：机主年龄 >=65 的客户对应的号码/服务订单。
- 时间：近三个月订单，默认先理解为按订单受理时间。
- 结果：输出所有订单明细，保留订单编码、号码、客户、年龄、受理时间、订单状态、动作类型、机构/揽装信息等。
- 默认假设：你说“所有订单”，所以默认不排除撤单/作废，也不只取竣工订单；年龄用 069 的身份证字段计算，不输出身份证原文。

还差这几个会影响 SQL 的点：

1. “近三个月”按哪个订单时间算：受理时间 act_date，还是竣工/状态时间 subs_stat_date？
2. 年龄按哪个时间点算：当前最新资料月年龄，还是订单发生当月的年龄快照？
3. 输出要订单明细即可，还是还要顺便按月/分局/营服汇总一版？

你确认这 3 点后，我下一步给你“方案确认”，确认完再出 SQL。

星期一16:04

1、受理时间，看202604到202606 ；2、按当前资料月算年龄；3、输出订单明细，字段：订单id，订单编码、号码、客户、年龄、受理时间、网点信息、揽装信息等

星期一16:08

方案确认

主表：

- 040 全业务号码订单表
- 用订单事实表取“所有订单明细”
- 当前表：zone_gz_yz.dwm_yz_rpt_comm_ba_subs_final
- 月表：zone_gz_yz.dwm_yz_rpt_comm_ba_subs_mon_final

时间口径：

- 按受理时间 act_date
- 查询窗口：2026-04-01 <= act_date < 2026-07-01
- 月表的 par_month_id 只是归档月，不当作受理月；SQL 会先合并订单池，再按 act_date 过滤。

补表：

- 补 069 全业务资料表 当前最新资料月算年龄
- 表：dwm_yz_tb_comm_cm_all_final
- 关联键：040.serv_id = 069.serv_id
- 年龄口径：social_id_type='1' 身份证，按当前年份减出生年份；不输出身份证原文。

过滤：

- age >= 65
- “所有订单”默认保留全流程状态：不加 subs_stat='301200'，不排除撤单/作废。

输出字段：

- 订单：subs_id、subs_code
- 号码/客户：serv_id、acc_nbr、cust_id、cust_nbr、cust_name
- 年龄：age、age_snapshot_month
- 时间/状态：act_date、subs_stat、subs_stat_reason、subs_stat_date
- 网点/机构：subst_id/subst_name、branch_id/branch_name、grid_id/grid_code
- 揽装：sales_code、sales_man_name、salestaff_id、salestaff_org_id、salestaff_subst_id、salestaff_branch_id、salestaff_channel_id

风险点：

- 年龄按当前资料月，不是订单发生当月年龄。
- 当前表 + 月表可能重复同一订单，SQL 会按 subs_id 去重，优先保留当前表记录。
- 069 当前月同一 serv_id 理论应唯一，但仍会附自检 SQL 看 JOIN 后是否放大。

你确认这个方案没问题，我下一步就生成完整 CTAS SQL。

星期一16:08

没问题

星期一16:09

确认收到，按刚才方案生成 SQL。这里用 202604-202606 受理时间窗口，即 2026-04-01 <= act_date < 2026-07-01。

```
-- 需求：取 202604-202606 受理订单中，机主年龄 >= 65 岁的所有订单明细 -- 主表：040 全业务号码订单表 -- 补表：069 全业务资料表当前最新资料月 -- 口径：所有订单，不默认排除撤单/作废，不默认只取竣工 -- 年龄：069 social_id/social_id_type 计算，不输出身份证原文 drop table if exists tmp_old65_order_pool_202606 purge; create table tmp_old65_order_pool_202606 row format delimited fields terminated by '\u0001' stored as orc tblproperties('orc.compression'='snappy') as select    t.src_type,    t.archive_month,    t.subs_id,    t.subs_code,    t.serv_id,    t.serv_nbr,    t.acc_nbr,    t.cust_id,    t.cust_nbr,    t.cust_name,    t.action_id,    t.action_type,    t.action_ex_type,    t.act_date,    t.subs_stat,    t.subs_stat_reason,    t.subs_stat_date,    t.subst_id,    t.subst_name,    t.branch_id,    t.branch_name,    t.grid_id,    t.grid_code,    t.sales_code,    t.sales_man_name,    t.salestaff_id,    t.salestaff_org_id,    t.salestaff_subst_id,    t.salestaff_branch_id,    t.salestaff_channel_id,    t.load_date from (    select        'mon' as src_type,        cast(a.par_month_id as string) as archive_month,        a.subs_id,        a.subs_code,        a.serv_id,        a.serv_nbr,        a.acc_nbr,        a.cust_id,        a.cust_nbr,        a.cust_name,        a.action_id,        a.action_type,        a.action_ex_type,        a.act_date,        a.subs_stat,        a.subs_stat_reason,        a.subs_stat_date,        a.subst_id,        a.subst_name,        a.branch_id,        a.branch_name,        a.grid_id,        a.grid_code,        a.sales_code,        a.sales_man_name,        a.salestaff_id,        a.salestaff_org_id,        a.salestaff_subst_id,        a.salestaff_branch_id,        a.salestaff_channel_id,        a.load_date    from zone_gz_yz.dwm_yz_rpt_comm_ba_subs_mon_final a    where a.par_month_id between 202604 and 202607      and substr(cast(a.act_date as string),1,10) >= '2026-04-01'      and substr(cast(a.act_date as string),1,10) <  '2026-07-01'     union all     select        'cur' as src_type,        cast(null as string) as archive_month,        a.subs_id,        a.subs_code,        a.serv_id,        a.serv_nbr,        a.acc_nbr,        a.cust_id,        a.cust_nbr,        a.cust_name,        a.action_id,        a.action_type,        a.action_ex_type,        a.act_date,        a.subs_stat,        a.subs_stat_reason,        a.subs_stat_date,        a.subst_id,        a.subst_name,        a.branch_id,        a.branch_name,        a.grid_id,        a.grid_code,        a.sales_code,        a.sales_man_name,        a.salestaff_id,        a.salestaff_org_id,        a.salestaff_subst_id,        a.salestaff_branch_id,        a.salestaff_channel_id,        a.load_date    from zone_gz_yz.dwm_yz_rpt_comm_ba_subs_final a    where substr(cast(a.act_date as string),1,10) >= '2026-04-01'      and substr(cast(a.act_date as string),1,10) <  '2026-07-01' ) t ; drop table if exists tmp_old65_order_dedup_202606 purge; create table tmp_old65_order_dedup_202606 row format delimited fields terminated by '\u0001' stored as orc tblproperties('orc.compression'='snappy') as select    x.src_type,    x.archive_month,    x.subs_id,    x.subs_code,    x.serv_id,    x.serv_nbr,    x.acc_nbr,    x.cust_id,    x.cust_nbr,    x.cust_name,    x.action_id,    x.action_type,    x.action_ex_type,    x.act_date,    x.subs_stat,    x.subs_stat_reason,    x.subs_stat_date,    x.subst_id,    x.subst_name,    x.branch_id,    x.branch_name,    x.grid_id,    x.grid_code,    x.sales_code,    x.sales_man_name,    x.salestaff_id,    x.salestaff_org_id,    x.salestaff_subst_id,    x.salestaff_branch_id,    x.salestaff_channel_id,    x.load_date from (    select        a.*,        row_number() over (            partition by a.subs_id            order by case when a.src_type = 'cur' then 1 else 2 end,                     a.load_date desc        ) as rn    from tmp_old65_order_pool_202606 a ) x where x.rn = 1 ; drop table if exists tmp_old65_cm_latest_202606 purge; create table tmp_old65_cm_latest_202606 row format delimited fields terminated by '\u0001' stored as orc tblproperties('orc.compression'='snappy') as select    x.par_month_id,    x.serv_id,    x.acc_nbr,    x.social_id_type,    x.social_id,    case        when length(x.social_id) = 18 and x.social_id_type = '1'            then cast(from_unixtime(unix_timestamp(),'yyyy') as integer) - cast(substr(x.social_id,7,4) as integer)        when length(x.social_id) = 15 and x.social_id_type = '1'            then cast(from_unixtime(unix_timestamp(),'yyyy') as integer) - cast('19' || substr(x.social_id,7,2) as integer)        else null    end as age from dwm_yz_tb_comm_cm_all_final x join (    select max(par_month_id) as par_month_id    from dwm_yz_tb_comm_cm_all_final ) m  on x.par_month_id = m.par_month_id ; drop table if exists ads_old65_order_detail_202606 purge; create table ads_old65_order_detail_202606 row format delimited fields terminated by '\u0001' stored as orc tblproperties('orc.compression'='snappy') as select    o.subs_id,    o.subs_code,    o.serv_id,    o.serv_nbr,    o.acc_nbr,    o.cust_id,    o.cust_nbr,    o.cust_name,    cm.age,    cm.par_month_id as age_snapshot_month,    o.act_date,    o.subs_stat,    o.subs_stat_reason,    o.subs_stat_date,    o.action_id,    o.action_type,    o.action_ex_type,    o.subst_id,    o.subst_name,    o.branch_id,    o.branch_name,    o.grid_id,    o.grid_code,    o.sales_code,    o.sales_man_name,    o.salestaff_id,    o.salestaff_org_id,    o.salestaff_subst_id,    o.salestaff_branch_id,    o.salestaff_channel_id,    o.src_type,    o.archive_month,    o.load_date from tmp_old65_order_dedup_202606 o join tmp_old65_cm_latest_202606 cm  on o.serv_id = cm.serv_id where cm.age >= 65 ;
```

自检 SQL：

```
select count(*) as order_rows,       count(distinct subs_id) as order_cnt,       count(distinct serv_id) as serv_cnt from ads_old65_order_detail_202606; select age_snapshot_month, count(*) as cnt from ads_old65_order_detail_202606 group by age_snapshot_month; select substr(cast(act_date as string),1,7) as act_month,       count(*) as cnt from ads_old65_order_detail_202606 group by substr(cast(act_date as string),1,7) sort by act_month; select subs_stat, subs_stat_reason, count(*) as cnt from ads_old65_order_detail_202606 group by subs_stat, subs_stat_reason sort by cnt desc; select count(*) as row_cnt,       count(distinct subs_id) as subs_cnt from tmp_old65_order_pool_202606; select count(*) as row_cnt,       count(distinct serv_id) as serv_cnt from tmp_old65_cm_latest_202606; select * from ads_old65_order_detail_202606 limit 20;
```

小提醒：月表归档月我这里裁到 202604-202607，是为了覆盖 4-6 月受理、稍后归档的订单；当前表也补了一遍未归档/当前池订单。